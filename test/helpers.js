// 测试共享的过程线与参数。

/** 先升后降的三角入流，带长尾让槽蓄泄空（洪量闭合用） */
export const TRIANGULAR_INFLOW = [
  0, 5, 10, 15, 20, 25, 30, 25, 20, 15, 10, 5, 0,
  ...Array(27).fill(0),
];

/** 短三角（失稳案例用，无需长尾） */
export const SHORT_TRIANGLE = [0, 10, 20, 30, 20, 10, 0];
